The component uses only simple html elements

The component returns the text Today's Weather.

Under this, information about today's weather is displayed. It 
gets this information from https://api.openweathermap.org